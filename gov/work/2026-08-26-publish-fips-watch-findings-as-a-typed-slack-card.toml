#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-08-26-001"
title = "Publish FIPS watch findings as a typed Slack card"
status = "done"
created = "2026-08-26"
started = "2026-08-26"
completed = "2026-08-27"
refs = ["RFC-0002:C-MARKDOWN-REPORT"]

[content]
description = "Extend the engine-owned report task with bounded, author-opted Slack rendering and make the existing fips-watch playbook deliver an actionable compliance report without exposing arbitrary Slack payloads or credentials. Structured result cards remain the default; a workflow may instead select an escaped Markdown output or explicitly bind selected outputs as template variables."

[[content.acceptance_criteria]]
text = "report(result = task) admits only a same-graph task and snapshots only its declared bounded JSON output after the main graph settles"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "the Slack destination renders engine-owned Block Kit with verdict, cost, selected result fields, task context, and a working run link"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "fips-watch reports its revision, clean and dirty variant counts, blockers, and filing outcome through the typed card"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "focused contract, broker, DSL, and FIPS workflow tests pass and govctl check is green"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "fips-watch explicitly selects card.markdown and Slack delivers it as escaped Markdown with a fallback while result-based cards remain compatible"
status = "done"
category = "changed"
