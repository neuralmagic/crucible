**`goal.md` carries an authoritative brief.** Its prescriptions are intentional — a human adopted
this implementation plan verbatim. If a fix requires touching `goal.md`, preserve the brief's
content; fix the gate or the controls instead, and never weaken the gate. Your reference fix (the
`good_cmd` patch under `_controls/`) stays private — the brief's prescriptions belong in the goal,
your `_controls/` do not.
